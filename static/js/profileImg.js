// Get the mobile span element
var mobileSpan = document.getElementById("mobile");
var emailSpan = document.getElementById("email");
var nameSpan = document.getElementById("name");
var fnameSpan = document.getElementById("Fname");
var mnameSpan = document.getElementById("Mname");
var lnameSpan = document.getElementById("Lname");
var ageSpan = document.getElementById("age");
var genderSpan = document.getElementById("gender");
var citySpan = document.getElementById("city");
var distSpan = document.getElementById("dist");
var pinSpan = document.getElementById("pin");
var countrySpan = document.getElementById("country");
var diseaseSpan = document.getElementById("disease");
var heightSpan = document.getElementById("height");
var DOBSpan = document.getElementById("DOB");

// Set the initial content as a placeholder
mobileSpan.textContent = "";
emailSpan.textContent = "";
nameSpan.textContent = "";
fnameSpan.textContent = "";
mnameSpan.textContent = "";
lnameSpan.textContent = "";
ageSpan.textContent = "";
genderSpan.textContent = "";
citySpan.textContent = "";
distSpan.textContent = "";
pinSpan.textContent = "";
countrySpan.textContent = "";
diseaseSpan.textContent = "";
heightSpan.textContent = "";
DOBSpan.textContent = "";

mobileSpan.addEventListener("input", function () {
    if (mobileSpan.textContent === "") {
        mobileSpan.textContent = "";
    }
});

emailSpan.addEventListener("input", function () {
    if (emailSpan.textContent === "") {
        emailSpan.textContent = "";
    }
});

nameSpan.addEventListener("input", function () {
    if (nameSpan.textContent === "") {
        nameSpan.textContent = "";
    }
});

fnameSpan.addEventListener("input", function () {
    if (fnameSpan.textContent === "") {
        fnameSpan.textContent = "";
    }
});

mnameSpan.addEventListener("input", function () {
    if (mnameSpan.textContent === "") {
        mnameSpan.textContent = "";
    }
});

lnameSpan.addEventListener("input", function () {
    if (lnameSpan.textContent === "") {
        lnameSpan.textContent = "";
    }
});

ageSpan.addEventListener("input", function () {
    if (ageSpan.textContent === "") {
        ageSpan.textContent = "";
    }
});

genderSpan.addEventListener("input", function () {
    if (genderSpan.textContent === "") {
        genderSpan.textContent = "";
    }
});

citySpan.addEventListener("input", function () {
    if (citySpan.textContent === "") {
        citySpan.textContent = "";
    }
});

distSpan.addEventListener("input", function () {
    if (distSpan.textContent === "") {
        distSpan.textContent = "";
    }
});

pinSpan.addEventListener("input", function () {
    if (pinSpan.textContent === "") {
        pinSpan.textContent = "";
    }
});

countrySpan.addEventListener("input", function () {
    if (countrySpan.textContent === "") {
        countrySpan.textContent = "";
    }
});

diseaseSpan.addEventListener("input", function () {
    if(diseaseSpan.textContent === "") {
        diseaseSpan.textContent = "";
    }
});

heightSpan.addEventListener("input", function () {
    if(diseheight.textContent === "") {
        diseheight.textContent = "";
    }
});

heightSpan.addEventListener("input", function () {
    if(heightSpan.textContent === "") {
        heightSpan.textContent = "";
    }
});